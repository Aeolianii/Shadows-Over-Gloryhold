# Next Task

## 2026-05-28 下一步

- 新视觉整合 Step 4 已完成。后续重点转为游戏页细节打磨：窄屏/低高度适配、右侧情报面板视觉统一、旧 CSS 清理。
- 新视觉整合 Step 3 已完成，下一阶段进入 Step 4：重构 `GamePage` 视觉，把 `./前端` 的“剧情事件台/指令输入”风格接入现有 WebSocket 日志流和发送方法。
- Step 4 必须继续保留现有 WebSocket hooks、公开/私密行动提交、推荐行动、玩家列表、角色情报和 world state 展示逻辑。
- Step 4 前建议先截图/记录当前游戏页状态，改动后重点验证：公开日志实时刷新、底部输入框可发送行动、推荐行动点击仍显示“AI GM裁定中”并触发真实行动。

## 2026-05-28 下一步

- 新视觉整合 Step 4 已完成。后续重点转为游戏页细节打磨：窄屏/低高度适配、右侧情报面板视觉统一、旧 CSS 清理。
- 新视觉整合 Step 3 已完成，下一阶段进入 Step 4：重构 `GamePage` 视觉，把 `./前端` 的“剧情事件台/指令输入”风格接入现有 WebSocket 日志流和发送方法。
- Step 4 必须继续保留现有 WebSocket hooks、公开/私密行动提交、推荐行动、玩家列表、角色情报和 world state 展示逻辑。
- Step 4 前建议先截图/记录当前游戏页状态，改动后重点验证：公开日志实时刷新、底部输入框可发送行动、推荐行动点击仍显示“AI GM裁定中”并触发真实行动。

## 最高优先级：移动端和细节抛光

目标：继续打磨 `./前端` 风格迁移后的响应式细节。

必须满足：
- 手机和平板下首页、选角页、游戏页不出现文本重叠或关键按钮离屏。
- 主界面保持原版视觉，不再套用 `./前端` 示例主题。
- 选角 7 槽头像镜框保持“仪式大厅”质感，同时继续校准移动端头像框对齐和标题箭头位置。
- 继续在不同桌面高度下检查选角页固定三段布局，避免底部卷宗内容过多时产生新滚动条。
- 后续新增前端样式优先使用 Tailwind v4 utility/CSS-first 能力，避免重新引入旧 PostCSS/Tailwind 配置；选角页旧 CSS 可继续逐步瘦身。

## 新视觉整合计划

- Step 1 已完成：合并 `./前端` 全局 CSS token，接入新版 AshParticles 算法。
- Step 2 已完成：只重构主界面的“开启封锁剧院”卡片按钮为新视觉风格，保留真实创建/加入房间逻辑。
- Step 3：重构 LobbyPage 视觉，绑定真实后端角色、占用、锁定与选角接口。
- Step 4：重构 GamePage 视觉，绑定真实 WebSocket 日志流和指令发送方法。
- 游戏页推荐行动、玩家列表、情报面板在窄屏下仍容易操作。
- 可逐步清理旧 CSS 中被主题覆盖但不再需要的段落。

## 后端已修复的问题

- 游戏中途 AI 无回应：原因为 AI 返回非法 `visibility=private` 导致 WebSocket 消息处理崩溃；现已清洗并保留连接。
- 调查进度 8/5：现已在状态写入、规则引擎和公开状态三层封顶到 5/5。
- 推荐行动：已新增玩家专属推荐行动 Agent 和前端面板，后续可继续优化推荐质量、节流和加载态。

## 后续任务

- 前端增加 GM/NPC 处理中状态。
- 推荐行动增加生成中的细粒度 loading 状态，避免 AI 慢响应时用户误以为空列表。
- 继续优化点名 AI 角色时的 GM/NPC 调用链。
- 缓存静态 persona/system prompt。
- 完成移动端 Cover Flow 选角版本。
